services:
  web:
    image: node:20
    ports:
      - "3000:3000"
    environment:
      - DATABASE_HOST=${DATABASE_HOST}
      - API_KEY=${API_KEY}
    depends_on:
      - redis
  redis:
    image: redis:7
